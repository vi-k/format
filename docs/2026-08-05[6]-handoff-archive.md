# Handoff от 2026-08-05 (архив)

**Это архив.** Актуальная точка входа — `HANDOFF.md` в корне; сюда
документ перенесён 2026-08-08 без правок. Разделы про состояние
устарели и текущим положением дел не являются: git-состояние другое,
CI в репозитории теперь есть, репозиторий формат-чист целиком, матрица
переехала из `example/` в `benchmark/suite/`, а арифметический поиск
паддинга для `{:0N,d}`, значившийся здесь отложенным, сделан. Ценность
документа — в истории решений и в подробностях замеров тех итераций.

Дата: 2026-08-05
Рабочий каталог: `/Users/user/development/my/format`

Все shell-команды запускать с префиксом `rtk` (hook подменяет
прозрачно). Спецификации, планы и handoff писать по-русски.

Перед каждым коммитом — `dart format` на затронутых файлах (локальный
хук `.git/hooks/pre-commit` проверяет это автоматически; в клоне его
надо ставить заново). Baseline-код в `benchmark/baselines/*` НЕ
форматировать и не менять: `format20` — замороженная копия,
`format16/lib/src/format_base.dart` — материализуется из pub скриптом
`benchmark/baselines/format16/fetch.dart` (в git не хранится; новый
клон обязан запустить скрипт один раз). Все закоммиченные файлы
формат-чисты; полный `dart format -o none .` показывает дрейф только
в материализованном файле 1.6 — это норма.

## Git-состояние

Ветка `main`. Ревью-раунд и реструктуризация (19 коммитов) запушены:
`origin/main` == `e8e9bc5`, первый живой прогон CI на нём зелёный.
Непушнутых коммитов ровно **шесть** — пост-ревьюшная закалка:

```text
<новый>  docs: prepare the changelog, record the hardening round
9b6ce5d  refactor: FIFO cache eviction, registry entries for low divergences
3da1c41  test: executable divergence registries, one test per fixture case
f6a1215  ci: bump actions/checkout to v5
fa77157  docs: record the dartSdk double-profile divergence and the 2.0.0 note
6e91fc8  docs: add dartdoc across the public API surface
--- граница origin/main: всё ниже УЖЕ в origin/main ---
e8e9bc5  chore: drop the Benchmark-all compound and its README mentions
```

Все коммиты содержат trailer `Co-Authored-By: Claude Fable 5
<noreply@anthropic.com>` и авторство `nashol`. Всё, что описано ниже,
закоммичено; эти пять коммитов в `origin` не пушились — владельцу
решать, когда пушить.

## Template IR: спека завершена (Tasks 1–11)

Редизайн форматтеров под IR (компиляция шаблона в программу op'ов,
прямая запись в общий буфер без промежуточных строк) полностью
реализован и провалидирован. Основные файлы:

- `lib/src/char_sink.dart` — `CharSink`: growable UTF-16 sink
  (`Uint16List`) с прямыми методами `writeCharCode`/`writeString`/
  `writeCodeUnits`/`fill`/`writeMagnitude`. **Новое в Task 11**: lazy
  single-string режим — поле `String? _single`; первый `writeString`
  на пустом sink'е просто запоминает строку по ссылке; любая
  последующая запись (второй `writeString`, `writeCharCode`, `fill`,
  `writeMagnitude`) сначала «материализует» отложенную строку в
  `_buffer` через тот же `setRange`-путь (метод `_materialize()`), а
  дальше работает как раньше. `toString()` для чисто-однострочного
  вывода возвращает `_single` НАПРЯМУЮ — ноль копий (быстрее даже
  legacy-пути с его одной копией через `StringBuffer`). `length`
  считает по `_single?.length ?? _length` (UTF-16 code units в обоих
  случаях). Инвариант: `_single != null` ⇒ `_length == 0`.
- `lib/src/template_ir.dart` — `_BraceOp`/`_PrintfOp` (sealed) и их
  компиляция:
  - Brace-op'ы: `_BraceLiteralOp` (статический текст), `_BraceFallbackOp`
    (всё, что не попало под классификацию — старый резолвер + одна
    строка), `_BraceDynamicValueOp` (`{}`/`{0}` без спецификации),
    `_BraceIntOp` (`d/b/o/x/X` без grouping/precision — пишет цифры
    сразу в sink через `writeMagnitude`), `_BraceTextOp` (`:s` с
    опциональными width/align/precision), `_BraceDoubleOp` (см. раздел
    «Double-op'ы» ниже).
  - Printf-op'ы: `_PrintfLiteralOp`, `_PrintfFallbackOp` (та же роль,
    что и brace-fallback: сохраняет `_staticResolved`/`_staticContext`
    мемоизацию), `_PrintfStringOp` (`%s`), `_PrintfIntOp`
    (`d/i/u/o/x/X`), `_PrintfDoubleOp` (см. ниже).
  - Компиляция тотальна и не бросает: неклассифицируемое поле всегда
    попадает в fallback-op, а не в ошибку компиляции.
  - Программа кэшируется по два слота на шаблон — `programFor(TextUnit)`
    в `_BraceTemplate`/`_PrintfTemplate` (`lib/src/brace_ast.dart`,
    `lib/src/printf_ast.dart`): один слот на `TextUnit.unicodeScalars`,
    один на `TextUnit.graphemeClusters`, лениво, максимум одна
    компиляция на комбинацию шаблон×юнит.
- `lib/src/brace_processor.dart` / `lib/src/printf_processor.dart` —
  `format()` прогоняет `program.ops` по общему `CharSink` и один раз
  зовёт `toString()`; `formatWithoutIr()` — сохранённый legacy-путь
  (сборка через `StringBuffer`), используется дифф-тестами и
  A/B-бенчмарком как baseline, публично не экспортируется.
- Seam'ы (не экспортируются `format.dart`, доступны только через
  `package:format/src/engine.dart`):
  - `debugCompiledProgramDescription(template, {printf, textUnit})` —
    список `op.describe()` скомпилированной программы, для
    компиляционных тестов.
  - `debugFormatBraceWithoutIr` / `debugFormatPrintfWithoutIr` — прямой
    вызов legacy-пути с теми же аргументами, что и публичный API, для
    дифф-тестов и A/B-бенчмарка.
  - `NumberLocale` реэкспортирован из `engine.dart` (Task 3) — чтобы
    дифф-тесты дотягивались до локалей без импорта `format.dart`.

## Double-op'ы: спека завершена (Tasks 1–5)

Резерв «double-op'ы», который предыдущий handoff оставлял открытым,
закрыт. Плавающие презентации больше не идут через fallback.

**Покрытие — оба диалекта плюс `{}`:**

- `_BraceDoubleOp` (`lib/src/template_ir.dart`) — brace-презентации
  `f F e E g G %`, а также **бестиповые** спецификации вида `{:10.3}`
  и `{:>10}`. Бестиповая ветка горячая **только для `double`**: legacy
  `_isFloatingFormatType(null)` возвращает `false`, поэтому для
  остальных типов op делегирует в общий `formatParsedValue`
  (для `String` под null-типом это легально даёт текстовый вывод).
- `_PrintfDoubleOp` — printf-презентации `%f %F %e %E %g %G`,
  включая динамические `*`-ширину/точность (отрицательная ширина
  сворачивается в флаг `left`).
- `_BraceDynamicValueOp` — double-ветка для пустой спецификации `{}`.
  Это самый быстрый случай: тела `_AsciiFloat` с шириной и
  выравниванием не строится вовсе, тело пишется в sink напрямую.
  На вебе целое `double` перехватывает int-ветка выше, поэтому в
  double-ветку там попадает ровно то, что отвергает `_isIntegerValue`
  (дробные, `-0.0`, `nan`, бесконечности) — так же маршрутизирует и
  legacy.

**Режим и локаль читаются на write, а не на компиляции.** Это ключевое
архитектурное решение итерации: `doubleFormatMode` и `numberLocale`
берутся из `frame.engine` в момент записи, поэтому на шаблон остаётся
**одна** скомпилированная программа, обслуживающая оба режима
(`dartSdk` и `compatible`) и любой движок. Проверено прямым probe'ом на
гейте: `{:.0f}` со значением `2.5` даёт `"3"` в `dartSdk` и `"2"` в
`compatible`, `{:e}` от `1.0` — `"1e+0"` против `"1.000000e+00"`,
printf `%.0f` от `2.5` — `"3"` против `"2"`; всё через один и тот же
op (`describe()` == `double:f:p0` / `double:e`).

**Общий писатель.** `_writeAsciiFloatDirect(sink, body, signChar,
percentSuffix, width, fillChar, align)` — единственная точка, которая
раскладывает готовое тело `_AsciiFloat` по sink'у: паддинг до знака
(`>`), половинный паддинг (`^`), знак, паддинг после знака (`=`), тело,
суффикс `%`, хвостовой паддинг. Используется **обоими** op'ами
(brace и printf) — сигнатура сведена в Task 1 и переиспользована в
Task 3 без изменений.

**Что осталось на fallback (сознательно):**

- `%a`/`%A` — hex-float; в hot-op не заводился.
- Нестандартные `numberLocale` (всё, что не `CNumberLocale`) —
  локализация цифр/знаков/разделителей воспроизводится legacy-хвостом
  по legacy-цене; проверка `identical(engine.numberLocale,
  const CNumberLocale())` стоит один `identical` на запись.
- Grouping (`{:,.2f}` и родственные) — остаётся на `_BraceFallbackOp`.

Компиляционная проверка на гейте (seam
`debugCompiledProgramDescription`): `{:.2f}`→`double:f:p2`,
`{:.0f}`→`double:f:p0`, `{:f}`→`double:f`, `{:e}`→`double:e`,
`{:g}`→`double:g`, `{:.3g}`→`double:g:p3`, `{:%}`→`double:%`,
`{}`→`dynamic`, `%.2f`→`double:f:p2`, `%e`→`double:e`,
`%g`→`double:g`; и наоборот `{:,.2f}`→`fallback`, `%a`→`fallback`.
Ни один измеряемый сценарий не ушёл в fallback молча.

**Порядок ошибок — контракт паритета.** Валидация precision в режиме
`dartSdk` идёт **до** проверки `isFinite`: плохая точность на `nan`
обязана бросить. Вердикт статический (`_rejectsDartDoublePrecision`
считается на компиляции), поэтому `FormatExceptionContext`
аллоцируется только на бросающем вызове. Порядок веток в `write`
обоих op'ов — транслитерация `formatBraceDouble`/`_formatPrintfDouble`;
это контракт, а не стиль.

**Чистка мёртвого контекста** (коммит `99ad663`): у
`_formatDartDouble` был неиспользуемый параметр `context`, ради
которого на каждой записи аллоцировался `FormatExceptionContext` в двух
местах (`template_ir.dart:110` и `:479`). Параметр и обе аллокации
удалены, legacy-вызовы обновлены — поведение не изменилось (неиспользуемость
подтверждена двумя ревьюерами).

## Hardening IR и parity-фаззер (связка из четырёх коммитов)

Связка закрыла **все пять** «точечных находок финального ревью ветки»,
которые предыдущий handoff оставлял в резервах. Ниже — что именно
сделано, по коммитам.

**`0ced01f` — проба валидаторов вместо копии правила (находки a и b).**
`_rejectsHotDouble` больше не воспроизводит правило `precision > 100000`
рукописно: он вызывает `_validateDoubleSpec` в `try/catch` и считает
отказом любое `FormattingException`. Правило теперь живёт в одном
месте, и классификатор не может разъехаться с валидатором — тот же
приём, что уже был у `_rejectsDartDoublePrecision`. Плюс в
body-switch'е `_PrintfDoubleOp` catch-all `_ =>` заменён на явную ветку
`'g' || 'G'` и `StateError` в `default`: если когда-нибудь заводить
`%a`, забытая ветка теперь упадёт громко, а не отформатирует молча не
тем генератором.

**`696eb2a` — запекание статических параметров (находка c).** Три
правки в hot-путях, все — перенос работы с записи на компиляцию:
- `staticPrecisionRejected` у `_PrintfDoubleOp`: вердикт валидатора
  precision для `dartSdk` считается при классификации. Динамическая
  `%.*f` вердикт на компиляции знать не может, поэтому сохраняет живую
  пробу (`precisionArgIndex < 0 ? staticPrecisionRejected : ...`) —
  это лучший оставшийся printf-выигрыш из ревью.
- `FormatExceptionContext` теперь аллоцируется **только** на реально
  отвергнутой precision, а не на каждой записи с precision.
- `uppercase` (`E`/`F`/`G`) запечён в поле op'а вместо пересчёта
  `type == 'E' || ...` на каждую запись — в обоих op'ах.
- `late final formatted` → `final formatted` в трёх местах: проверка
  инициализации на записи убрана (все ветки присваивают ровно один раз).

Гейт этого коммита — **A/B-прогоны**, не полная матрица: правки
микроскопические, а A/B чувствительнее (порог 5% против порога
обнаружения ~9% у межсессионной сверки) и меряет IR против legacy в
одном процессе. Ноль `RESULTS DIFFER`, ноль `LEGACY FASTER`.

**`df6ac9b` — расширение дифф-матрицы (находка d).** Шесть новых
парити-кейсов: brace-double под кастомной (не-`C`) локалью — та самая
непокрытая комбинация из ревью; `{}`-спецсзначения при
`doubleSpecialValueSpelling: short`; краевые значения double
(включая `-0.0`-класс и переполнение в бесконечность); printf-double
на графемном движке и с нехваткой аргументов; отказ по precision в
`dartSdk` — и для статической, и для динамической `%.*f`. Заодно
`lib/src/engine.dart` доэкспортировал `DoubleSpecialValueSpelling` в
seam-поверхность (рядом с `DoubleFormatMode` и `TextUnit`), чтобы
дифф-тесты строили движки во всех спеллингах без импорта `format.dart`.

**`4128a3d` — parity-фаззер (находка e, самый выгодный тестовый вклад).**
Общий парити-харнес вынесен из дифф-теста в `test/parity_harness.dart`
(`expectBraceParity` / `expectPrintfParity` / `expectContextParity` —
имена сохранены, поэтому дифф-тест правился импортами и удалением
локальных копий: `+28/−151`, то есть чистые −123 строки). Заодно тем же
коммитом в дифф-тест добавлен **один новый кейс** — `brace dartSdk
precision rejection keeps parity`: границы precision в `dartSdk`
(`[0,20]` для `f/F/e/E`, `[1,21]` для `g/G`, плюс пустая конверсия
(g-образец `[1,21]`) и `%` (f-образец `[0,20]`)), обе стороны границы,
на `_specialDoubles` и в обоих double-режимах. Поверх харнеса —
`test/template_ir_fuzz_test.dart`:

- **seed `20260805`, литеральный** — корпус воспроизводится в пределах
  одного рантайма и, **после `7dd02d5`, одинаков на VM и на node**: оба
  генератора перешли на `pow(10.0, ...)` (double-основание), поэтому
  показатель 19 больше не переполняет int64 на VM. До этого расходилось
  **~0.6% выборок `_value`** — величина аналитическая: 2 ветки из 8
  зовут `pow`, показатель 19 — 1 из 40, итого 0.625% (замер на 2000
  выборках дал 6 расхождений, это ~1.9σ ниже среднего — по одной такой
  выборке рейт оценивать нельзя). После refresh'а — 2000 подряд
  совпадают численно на обоих рантаймах. Менять seed только осознанно и с
  комментарием. Четыре теста берут `_seed`, `_seed + 1`, `_seed + 2`,
  `_seed + 3`.
- **1600 кейсов** = `_casesPerDialect` 400 × 4 теста (brace и printf,
  каждый в варианте «случайное значение» и «значение, подобранное под
  спецификацию»).
- **6 движков** — `defaultFormat`, `graphemeFormat`, `compatibleFormat`,
  `compatibleGraphemes`, `localeFormat`, `shortSpellingFormat`; движок
  выбирается на кейс, так что один прогон обходит горячие op'ы,
  графемные fallback'и, `compatible`-режим, локализованный хвост и
  короткие спеллинги.
- **Невалидные спецификации не фильтруются, а желательны**: оба пути
  обязаны отвергать их одинаково, вместе с содержимым контекста
  исключения.
- **Гейты на вырождение корпуса.** Distinctness: уникальных шаблонов
  `> _casesPerDialect ~/ 4` (во всех четырёх тестах). Плюс
  **success floor** в двух «matched»-тестах: реально отрендерившихся
  кейсов `> _casesPerDialect ~/ 2`. Второй гейт существует именно
  потому, что одной distinctness мало — корпус, целиком выродившийся в
  ошибки, остался бы разнообразным на вид, но перестал бы фаззить
  пути рендеринга.
- Стоимость: **~0.84 с на VM** (замер этой сессии), 4 теста; на node
  тоже проходит.

## Чистка бенчмарков (связка из трёх коммитов)

Два функциональных коммита ниже плюс сам docs-коммит `568a639`, который
их записал, — вся связка запушена. Кода библиотеки она не касается —
только бенчмарк-инструментов, их тестов и `.vscode/launch.json`.

**`3905650` — format2-гейт снят как избыточный.** Его роль PASS/FAIL
перекрыта более строгим релизным харнесом (`benchmark/runner.dart` +
`gates.dart`) и чувствительной A/B-матрицей `template_ir_benchmark`, а
визуальную роль (посмотреть на 2.0 против 3.0) выполняет матрица
`benchmark.dart`; после IR его порог 1.02 на мульти-плейсхолдерах не
срабатывает вообще. Удалены `example/bin/format2_gate_benchmark.dart`,
`example/lib/format2_gate_benchmark.dart`, каталог
`example/lib/src/format2_gate/` (4 файла) и
`example/test/format2_gate_benchmark_test.dart`; из `launch.json` убрана
конфигурация «Benchmark: Format 2 gate» и её строка в compound
«Benchmark: all»; из `example/README.md` — команда запуска.

Единственный сценарий, который был **только** у гейта, — 50
авто-нумерованных плейсхолдеров — перенесён в матрицу
(`example/lib/src/tests/tests.dart`) обычным `BenchmarkScenario` по
образцу соседнего 10-плейсхолдерного: шаблон `'{}'` ×50 через `|`,
sprintf-сторона `'%d'` ×50 через `|`, значения `0..49`, ни один движок
не пропускается. Сценариев в матрице стало **27** (было 26);
интеграционный тест `example/test/restored_benchmark_test.dart` пинит
новое число, форму перенесённого сценария (отдельным тестом) и его
появление в живом выводе, а ожидание «ровно один намеренный ERROR»
осталось прежним.

**`5b087aa` — probe `benchmark/parser_strategy.dart` помечен как
архивный.** Это одноразовый design-probe: он один раз сравнил три
парсера sprintf и выбрал победителя. Решение записано в
`benchmark/results/parser_strategy.json` (`selected: scanner`, коммит
`682c37f`, 2026-08-02) и реализовано в парсере пакета; кэш шаблонов и
IR потом вообще убрали парсинг с горячего пути, так что повторный
прогон ничего изменить не может. Закрыт именно **вопрос probe'а** (какая
стратегия), а не тема «производительность парсера»: там, где парсер ещё
работает, его меряет холодная секция матрицы
(`example/lib/src/benchmark_cold.dart` — каждый вызов идёт по шаблону,
которого кэш не видел). В шапку файла добавлен комментарий об
этом, **код не тронут** — probe остаётся запускаемым ради
происхождения. Из compound «Benchmark: all» он убран (рутинные прогоны
остаются на живых бенчмарках), отдельная конфигурация
«Benchmark: parser strategy JIT» сохранена.

Живой прогон матрицы после переноса (`dart run example/bin/benchmark.dart`,
quick): **41.6 с** (порог 60 с), ERROR ровно один — намеренный
(`sprintf 7.0` на минимальном int). Перенесённая строка:
`sprintf 7.0` 65.032 µs, `format 2.0` 23.733 µs, `format 3.0 → format`
**2.058 µs**, `format 3.0 → sprintf` **2.800 µs**, все четыре `OK`.
Это функциональная проверка (сценарий виден, вывод сходится), а не
гейт: `vm.loadavg` был `5.06` на старте и `4.60` на финише, то есть
чуть выше проектного порога `< 5`, и сравнивать эти абсолютные числа с
чем-либо не следует. Порядок величин, впрочем, ожидаемый — на 50
плейсхолдерах 3.0 быстрее 2.0 примерно в 11 раз и sprintf 7.0
примерно в 32 раза.

## Follow-up связка (четыре коммита, непушнутая)

Три пункта из «отложенных мелочей» прошлого handoff'а закрыты, четвёртый
коммит — документация. Продакшн-код (`lib/`) правился только в
`engine.dart` (seam-экспорт), поведение не менялось.

**`31e2659` — payload'ы исключений в харнесе.** Сравнение
`irError.toString()` с `legacyError.toString()` было **вакуумным**:
`FormattingException` не переопределяет `toString()`, поэтому строка
сверяла ровно то же, что и соседняя проверка `runtimeType`. Поля
подклассов (`reason` / `key` / `segment` / `value` / `matches` /
`extension`) не сравнивались ничем — две разные отбраковки одного типа с
одинаковым контекстом были неразличимы. Добавлен
`describeErrorPayload()` — `switch` по sealed-иерархии **без
`default`-ветки**, так что новый подкласс в `lib/src/errors.dart` сломает
компиляцию теста, а не выпадет из сравнения молча. Значения описываются
вместе с `runtimeType` и под защитой от бросающего `toString()`
(сравнение payload'ов не должно превращаться в падение чужого
`toString()`); у `FormatExtensionException` сравнивается **только имя
расширения** — обёрнутая ошибка и стек приходят из пользовательского
кода. Хелперы получили опциональный `label`; фаззер кладёт туда индекс
кейса, индекс движка и значение, поэтому падение триажится по сообщению,
а не по seed'у. `engine.dart` доэкспортировал конкретные типы исключений
(их обязан уметь называть `switch`), из-за чего импорт
`package:format/format.dart` в `parser_test.dart` и
`printf_parser_test.dart` стал избыточным и убран — оба теста теперь
целиком на seam'е. **Расхождений IR/legacy новое сравнение не нашло** ни
на VM, ни на node.

**`7dd02d5` — corpus refresh фаззера.** `pow(10, e)` → `pow(10.0, e)` в
обоих генераторах. Смена корпуса равносильна смене seed'а, поэтому
отдельным коммитом и с пере-проверкой гейтов на новых числах: уникальных
шаблонов **390 / 327 / 383 / 346** при полу `> 100`, отрендеренных
**283** и **288** при полу `> 200` — числа не изменились: сдвинулись
только отбраковываемые VM-выборки в первых двух тестах (шаблоны рисует
свой поток, `pow` случайности не потребляет), а `_matchedValue` до
показателя 19 не доходил вовсе. Проверено на обоих рантаймах. Рейт
расхождения до refresh'а — **~0.6% аналитически** (2/8 × 1/40 = 0.625%);
разовый замер на 2000 выборках дал 6, и по нему рейт занижался вдвое —
исправлено отдельным docs-коммитом.

**`a95fac4` — граница precision у `%`.** В rejection-матрицу дифф-теста
добавлены `'{:.21%}'` (f-образец отвергает, g-образец принял бы) и
`'{:.0%}'` (f-образец принимает, g-образец отверг бы) — ровно те два
кейса, что различают образцы; прежние `.25%`/`.20%` лежали в зоне, где
образцы совпадают (проверено вживую: `{:.21%}` →
`InvalidSpecifierException`, `{:.0%}` → `250%`). Заодно исправлен
комментарий блока: он утверждал, что
пустая конверсия и `%` идут через одну валидацию, а в
`_validateDartDoublePrecision` `general` покрывает только `null/g/G/n` —
пустая конверсия g-образная `[1,21]`, `%` f-образная `[0,20]`.

## Ревью и багфикс-раунд (2026-08-05, вечер)

- Проведено полное независимое ревью проекта; отчёт —
  `docs/2026-08-05[4]-project-review.md` (5 High, 13 Medium, 17 Low,
  приоритизированный план действий внутри). Вся документация
  перенесена в плоский `docs/` (`YYYY-MM-DD[N]-name`, планы — суффикс
  `-plan`); новые спеки/планы/отчёты сохранять туда же. Появился
  `TODO.md` — рабочий бэклог, правила в его шапке.
- `fix` (`6da4107`) — пять кодовых находок ревью, все по TDD
  (RED → GREEN, паритет IR ↔ legacy сохранён, фаззер зелёный):
  1. `{:0N,d}`: нули подгоняются к сгруппированной ширине
     (`fitRegroupedZeroPadding` в `formatBraceInteger`) — паритет с
     Python и format 2.0; **три старых пина закрепляли баг** и
     обновлены (`{:08_x}`, `{:#010_x}` в двух файлах).
  2. dart2js: `{:d}`/`%d` за 2^53 — точные цифры через `BigInt` в
     `_formatIntMagnitude`; закрыты `1e+21`, `1e,+21` и неточные
     хвосты; проверено живым прогоном на node.
  3. `toString()` у всей sealed-иерархии исключений: исчерпывающий
     switch без default, защита от бросающего `value.toString()`.
  4. Лимит ширины 100000 в brace-пути (в т.ч. динамической `{:{}d}`);
     константа переименована в `_maximumSafeFormatOption` — общий
     контракт printf и brace.
  5. printf `%s` сохраняет причину упавшего `toString()`
     (`FormatExtensionException` с error и stackTrace, как brace-путь).
- Перф проверен до/после в одной сессии (stash-приём, loadavg 3.0):
  A/B — все сценарии в шуме ±5–8%, горячие пути не задеты
  (выброс `%s` +6.7% опровергнут повторным прогоном). Осознанная
  цена: `{:010,d}` 0.65 → ~1.6 µs/op на холодном fallback; план
  ускорения — в «Возможные следующие шаги», короткий пункт — в
  `TODO.md`.
- `{:010,d}` заведён сценарием в оба бенчмарка (`65e8c9e`): 16-й в
  A/B (fallback-control, EQUAL 0.0%) и 28-й в полной матрице
  (format 2.0 согласен: `00,001,234` — фикс №1 был регрессией 3.0
  относительно 2.0).
- Гигиена (`1b886b4`): pub-архив 189 → 127 KB (из публикации
  исключены `HANDOFF.md`, `TODO.md`, `benchmark/`, `tool/`,
  `example2/`); `example/bin/benchmark.exe` (5.7 MB) вычеркнут из
  git (файл остался на диске); мёртвый `dart_code_metrics` и
  Flutter-excludes убраны из `analysis_options.yaml` (вывод
  анализатора не изменился); rtk-префиксы сняты с публикуемых README
  бенчмарков.
- **Поправка к находке M5 ревью**: пин Node `v24.8.0` в
  `benchmark/gates.dart` — НЕ «мёртвый гейт». README results
  (строки 66–67) документирует запуск через
  `npx node-bin-darwin-arm64@24.8.0` при системной другой версии —
  это осознанный воспроизводимый дизайн. Код гейта не менялся.
- ~~CI~~ — **закрыт** (`4b7f96e`): GitHub Actions
  `.github/workflows/ci.yaml`, два job'а (checks: формат, zero-info
  analyze lib/test/example, полный VM-сьют, node-набор из 57 тестов
  включая js_number_dispatch, пример, format_intl, pub dry-run;
  benchmark-suite: fetch format16 из pub → pub get suite → полный
  analyze → тесты сьюта). Каждый шаг верифицирован симуляцией свежего
  клона (полный analyze возможен только после fetch+resolve — в
  свежем клоне без них 102 uri-ошибки, поэтому он в suite-job'е).
  Живой прогон на GitHub увидим после пуша.
- Осталось из плана ревью: ничего блокирующего — все High и Medium
  закрыты (см. раздел «Закалка после ревью» ниже).

## Закалка после ревью (2026-08-05, ночь)

- **dartdoc** (`6e91fc8`): вся публичная поверхность задокументирована,
  `dart doc` — 0 warnings/errors.
- **Реестры дивергенций стали исполняемыми** (`3da1c41`): каждая
  запись обоих реестров обязана иметь исполняемый экземпляр,
  пиняющий заявленное Dart-поведение (python: 10 id, JS-запись —
  явное платформенное исключение, она пинится js_number_dispatch на
  node; sprintf: 8 result- + 6 error-экземпляров). Тест равенства
  id ↔ экземпляры не даст добавить запись без пина. Корпуса (56
  python + 41 sprintf) разбиты на test() на кейс; загрузчики стали
  синхронными фабриками. Корень: 401 → 519 тестов.
- **Новые записи реестров** (`9b6ce5d`): юникод-цифры в
  width/precision (отказ), `{:c}` на одиночном суррогате и с
  нулевым заполнением (отказ), spelling subnormal в `%a`
  (фиксированная экспонента p-1022, glibc-стиль); запись
  `dartsdk-default-double-profile` (`fa77157`) + пример `%e` в README
  и заметка о непубликовавшейся 2.0.0 в разделе миграции.
- **Кэш шаблонов: FIFO-вытеснение** (`9b6ce5d`) вместо обвального
  сброса: переполнение вытесняет одну старейшую запись, хиты без
  бухгалтерии (горячий путь не тронут), неудачный парс ничего не
  вытесняет; ёмкость — через шов `debugTemplateCacheCapacity`;
  printf-кэш покрыт тестом переполнения.
- Мёртвая ветка `formatParsedValue` и два её хелпера удалены; единый
  источник зарезервированных имён (`_builtInTypes`);
  `_staticBraceSpecification` больше не дублируется.
- CHANGELOG подготовлен к релизу: заголовок «2.0.0 (unpublished)»,
  вводная фраза для мигрирующих с 1.6.0, потолок 100000, точные
  web-цифры, dartdoc.
- Расширение фаззера отложено в TODO.md с объёмом (нужна пересъёмка
  порогов корпуса на VM и node).

## Реструктуризация и подготовка к публикации (2026-08-05, поздний вечер)

- **`example/` → `benchmark/suite/`** (`f26d0a4`): бывший example-пакет
  был бенчмарк-сьютом; теперь все бенчмарки живут под `benchmark/` —
  гейт-харнес сверху, ANSI-матрица в `suite/` (имя пакета
  `format_benchmarks`, импорты `package:format_benchmarks/...`,
  path-зависимость sprintf7 стала `../baselines/sprintf7`). VS Code
  конфигурации перенацелены; исторические упоминания
  `example/bin/...` в разделах ниже оставлены как были — теперь это
  `benchmark/suite/bin/...`.
- **Настоящий пример**: `example/format_example.dart` — гид по API
  (braces, printf, настроенные Format, кастомный форматтер,
  типизированные ошибки), комментарии совпадают с реальным выводом
  (проверено запуском); pub.dev покажет его на вкладке Example.
  Скудный `example2/` удалён.
- **Формат-нормализация** (`639d752`): весь репозиторий отформатирован
  SDK 3.12.2; в дрейфе остались ровно 3 verbatim-файла
  `benchmark/baselines/sprintf7` (намеренно). Локальный пре-коммит хук
  `.git/hooks/pre-commit` проверяет формат затронутых dart-файлов
  (baselines исключены). **Решение по SDK**: минимум `^3.7.2` НЕ
  поднимаем ради форматтера — tall style гейтится языковой версией
  (3.7+ уже включён), дрейф был от разницы реализаций форматтера
  между SDK, а не от языковой версии; лечится форматированием одним
  актуальным SDK + хуком/CI.
- **pub.dev** (`89f48dd` + `f26d0a4`): topics у обоих пакетов; архив
  111 KB; dry-run — 0 предметных warnings (остались только «грязное
  дерево» на момент прогона и hint о скачке версии 1.6.0 → 3.0.0).
- Проверки после реструктуризации: корень 401, `benchmark/suite` 14,
  `format_intl` 7 — все зелёные; analyzer — те же 13 info в
  `benchmark/` (baselines/model/scenarios), lib/test чисты.
- **sprintf7 теперь из pub** (`f038f7e`): вендоренный
  `benchmark/baselines/sprintf7` удалён; вместо него точный пин
  `sprintf: 7.0.0` (dev-зависимость корня для гейт-харнеса,
  зависимость suite для матрицы). Перед удалением `lib/` вендора
  сверен побайтово с pub-пакетом — **идентичен**, сопоставимость
  записанных бенчмарк-чисел сохранена; provenance раннера теперь
  `7.0.0/pub`. Пин точный намеренно: бамп версии обнуляет
  сопоставимость. `format2` остаётся вендоренным — версия 2.0.0
  никогда не публиковалась, из pub её не взять.
- **format 1.6.0 в матрице бенчмарков**: pub-версию нельзя взять
  зависимостью напрямую (коллизия имени `format` с локальным пакетом),
  поэтому в `benchmark/baselines/format16/` лежит тонкий
  пакет-обёртка `format16_baseline`, а сама реализация (один файл
  `lib/src/format_base.dart`) НЕ коммитится — материализуется из
  pub-кэша скриптом `fetch.dart` (pub сам верифицирует хэш архива;
  SHA-256 записан в README baseline'а). Раннер
  `BenchmarkFormat16Format` — шестая колонка матрицы; расхождения 1.6
  сняты пробой по всем 28 сценариям: их ровно два (`{:e}` —
  6-значная мантисса по умолчанию, как у 2.0/sprintf7; `{:%}` — тип
  не поддержан, шаблон остаётся литералом, как у 2.0), оба закрыты
  `skipFormat16` с Oracle-комментариями; намеренный ERROR в матрице
  по-прежнему ровно один. Примечательно: `{:010,d}` и minInt у 1.6
  корректны.
- **Версионные суффиксы в именах раннеров**: по аналогии с format16 —
  `BenchmarkSprintf70`, `BenchmarkFormat20Format`,
  `BenchmarkFormat30Format`/`Sprintf`/`Cold*`, файлы
  `benchmark_sprintf70.dart` и т.д., флаги
  `isFormat20`/`skipFormat20`, `isSprintf70`/`skipSprintf70`,
  `isFormat16`/`skipFormat16`; каталог `baselines/format2` →
  `baselines/format20` (обновлены `part of` в его src),
  `legacy_format_baseline.dart` → `format20_baseline.dart`; ключи
  provenance раннера — `format20Baseline`/`sprintf70Baseline`.
  Отображаемые имена в отчётах не менялись (`format 2.0 → format`).
- **format 2.0 удалён из визуальной матрицы** (по решению владельца):
  никогда не публиковавшаяся 2.0 ничего не добавляет рядом с pub-1.6;
  колонка `format 2.0 → format` и 825-строчная приватная копия движка
  2.0 в suite (`format20_baseline.dart` — та самая
  «вторая вендоренная копия» из ревью, M11) удалены; флаг
  `skipFormat20` упразднён; suite похудел на зависимости
  `characters`/`intl`/`meta`. **Релизный гейт НЕ тронут**:
  `benchmark/baselines/format20` остаётся его откалиброванным
  baseline'ом и контрольной группой шума — пороги ≤1.02/≤1.05 сняты
  относительно него.
- **Локаль-витрина: kk_KZ вместо uk_UA** (`4cdf9a1`): README-примеры,
  `example/format_example.dart` (заполнение `🇰🇿`, слово
  `Қазақстан`), format_intl (README, example, тесты), golden-intl
  сценарий бенчмарка и его пин. CLDR-символы kk совпадают с uk
  (NBSP-группировка, запятая, латинские цифры) — все ожидаемые
  строки не изменились, проверено живыми прогонами.

## Замеры: RED → GREEN

### A/B (IR против legacy на идентичных вызовах)

Команда: `dart run example/bin/template_ir_benchmark.dart`
(`example/lib/src/template_ir_benchmark.dart`,
`example/bin/template_ir_benchmark.dart`).

**Состав матрицы вырос до 15 сценариев** (было 10): 13 hot + 2
fallback-control. Добавлены пять double-строк (`{:.2f}`,
`{:.2f} compatible`, `{:e}`, `{} (double)`, `%.2f`); бывший
fallback-control `{:.2f}` стал hot-сценарием, а его место в контроле
занял `{:,.2f}` (grouping) со значением `1234567.891` — на `3.14159`
разделитель не появился бы вовсе, и контроль был бы пустым.

RED этой итерации (до double-op'ов): все double-презентации шли через
fallback, то есть IR и legacy исполняли один и тот же код и давали
`PERFORMANCE EQUAL` (`{:.2f}`: EQUAL 1.3% / 1.2%).

GREEN — два живых прогона, `vm.loadavg` `4.07` и `3.93`, **ноль
`RESULTS DIFFER` и ноль `LEGACY FASTER` в обоих**:

| Сценарий | IR (1 / 2) | Legacy (1 / 2) | Вердикт (1 / 2) |
|---|---|---|---|
| `{:10d}` (hot) | 0.081 / 0.080 µs | 0.242 / 0.252 µs | IR FASTER 198.6% / 215.0% |
| `{:x}` (hot) | 0.058 / 0.056 µs | 0.171 / 0.185 µs | IR FASTER 193.4% / 228.1% |
| `{:s}` (hot) | 0.031 / 0.032 µs | 0.064 / 0.069 µs | IR FASTER 102.8% / 116.5% |
| `{:<10s}` (hot) | 0.141 / 0.141 µs | 0.144 / 0.142 µs | EQUAL 2.0% / 0.6% |
| `{}` (hot) | 0.033 / 0.031 µs | 0.066 / 0.065 µs | IR FASTER 99.3% / 110.2% |
| **`{:.2f}` (hot, double)** | 0.124 / 0.130 µs | 0.578 / 0.595 µs | **IR FASTER 366.2% / 359.2%** |
| **`{:.2f}` compatible (hot, double)** | 0.132 / 0.136 µs | 0.570 / 0.576 µs | **IR FASTER 332.9% / 321.7%** |
| **`{:e}` (hot, double)** | 0.147 / 0.150 µs | 0.955 / 0.961 µs | **IR FASTER 547.4% / 539.1%** |
| **`{}` (double) (hot)** | 0.044 / 0.043 µs | 0.548 / 0.559 µs | **IR FASTER 1141.8% / 1185.0%** |
| `%s` (hot) | 0.069 / 0.068 µs | 0.107 / 0.100 µs | IR FASTER 55.4% / 46.5% |
| `%10d` (hot) | 0.128 / 0.127 µs | 0.274 / 0.284 µs | IR FASTER 114.6% / 123.2% |
| `%0*d` (hot) | 0.128 / 0.135 µs | 0.372 / 0.366 µs | IR FASTER 191.2% / 170.9% |
| **`%.2f` (hot, double)** | 0.171 / 0.174 µs | 0.613 / 0.649 µs | **IR FASTER 258.6% / 272.0%** |
| `{:,.2f}` (fallback-control) | 0.808 / 0.814 µs | 0.798 / 0.799 µs | EQUAL 1.3% / 1.9% |
| `{:é^10s}` (fallback-control, grapheme) | 0.213 / 0.214 µs | 0.216 / 0.214 µs | EQUAL 1.7% / 0.0% |

Диапазон по пяти double-строкам — **от +259% до +1185%**, и он
**неравномерный**: усреднять его нельзя. `{} (double)` выигрывает на
порядок больше остальных именно потому, что пустая спецификация не
строит обвязку с шириной и выравниванием. Оба контроля остались
`EQUAL` — hot-путь не задел fallback.

`{:<10s}` (hot) по-прежнему `EQUAL` — унаследовано от предыдущей
итерации (там было 2.8% / 2.1%), не регрессия, но и не сдвинулось за
две спеки.

### Полная матрица (`example/bin/benchmark.dart`)

Команда: `dart run example/bin/benchmark.dart` (quick-режим), три
прогона. Сырые выводы лежат в `red-benchmark.txt` (RED) и
`green-benchmark.txt` (GREEN, третий прогон, 39.8 с) внутри
`.superpowers/sdd/2026-08-05-double-ops/`. **Внимание: этот каталог
целиком под `.superpowers/sdd/.gitignore: *` — файлы локальные, в git
их нет и в другой машине/клоне они не появятся.** Поэтому все числа, на
которые опираются выводы, продублированы прямо в этом HANDOFF: таблицы
ниже самодостаточны, сверять по ним можно без доступа к сырым файлам.
Числа в таблицах ниже — прогоны 1 и 2; третий
прогон сошёлся с ними построчно и оказался чуть лучше (машина была
тише: сдвиг контрольной группы +6.3% против +8.8..+9.9%), например
`{:g}` 7.03x против 6.65–6.74x, `{:%}` 2.93x против 2.78–2.82x.

**Сначала — поправка на шум, без неё таблицы читать нельзя.** RED снят
в другой сессии на более тихой машине. Это видно по раннерам, которых
наш код вообще не касается (`sprintf 7.0` — сторонний пакет,
`format 2.0` — старый пакет): 52 контрольные строки сдвинулись на
**+5.6..+14.2% (среднее +9.9%)** в прогоне 1 и **+5.9..+12.2%
(среднее +8.8%)** в прогоне 2. То есть у машины сегодня встречный ветер
около +9%.

Double-секции, `format 3.0 → format` / `format 3.0 → sprintf`, µs/оп:

| Шаблон | Значение | Раннер | RED | GREEN 1 / 2 | Ускорение |
|---|---|---|---|---|---|
| `{:.2f}` | `0.1` | format | 0.533 | 0.127 / 0.127 | **4.20x** |
| `{:.2f}` | `0.1` | sprintf | 0.543 | 0.161 / 0.164 | **3.31–3.37x** |
| `{:.2f}` | `12345678901234.568` | format | 0.593 | 0.149 / 0.147 | **3.98–4.03x** |
| `{:.2f}` | `12345678901234.568` | sprintf | 0.601 | 0.182 / 0.182 | **3.30x** |
| `{:f}` | `3.141592653589793` | format | 0.540 | 0.130 / 0.130 | **4.15x** |
| `{:f}` | `3.141592653589793` | sprintf | 0.543 | 0.156 / 0.155 | **3.48–3.50x** |
| `{:e}` | `12345.6789` | format | 0.885 | 0.174 / 0.173 | **5.09–5.12x** |
| `{:e}` | `12345.6789` | sprintf | 0.867 | 0.189 / 0.196 | **4.42–4.59x** |
| `{:g}` | `0.00012345` | format | 0.492 | 0.073 / 0.074 | **6.65–6.74x** |
| `{:g}` | `0.00012345` | sprintf | 0.489 | 0.103 / 0.104 | **4.70–4.75x** |
| `{:.3g}` | `1234567890123.0` | format | 0.884 | 0.145 / 0.145 | **6.10x** |
| `{:.3g}` | `1234567890123.0` | sprintf | 0.879 | 0.182 / 0.184 | **4.78–4.83x** |
| `{:%}` | `0.756` | format | 0.604 | 0.217 / 0.214 | **2.78–2.82x** |

**Честный диапазон: −64.1% (`{:%}`) … −85.2% (`{:g}`)** по 13 строкам.
Он неравномерный: `{:%}` — самый слабый выигрыш (2.8x), `{:g}` и
`{:.3g}` — самый сильный (6.1–6.7x). Свести это к одному «~4x» было бы
приукрашиванием. Обе крайние точки воспроизвелись в обоих прогонах.
Улучшение идёт **против** встречного ветра +9%, то есть
нормализованный выигрыш ещё больше сырого.

Не-double сценарии (43 строки `format 3.0`; в отчёте Task 5 стояло 45 —
две cold-строки были посчитаны дважды, они разбираются отдельно ниже):
**+0.0..+17.9% (среднее
+8.6%)** и **−0.9..+14.3% (среднее +8.8%)**. Среднее совпадает со
средним нетронутых сторонних раннеров (+9.9% / +8.8%) — не-double путь
движется ровно вместе с машиной. Худшие отдельные строки — самые мелкие
абсолютные величины в матрице (`{}` `hello world` 0.028→0.033 µs,
`{:c}` 0.100→0.115 µs), где шаг округления вывода 0.001 µs сам даёт
1–4%.

**Точная формулировка вывода (важно не переусиливать).** Межсессионная
сверка с RED-файлом даёт не «регрессии нет», а «регрессии выше порога
обнаружения ~9% нет»: разброс контрольной группы сам по себе ±9%, и
регрессию меньше этой величины данный инструмент увидеть не может.
Для сравнения — собственный порог эквивалентности A/B-гейта 5%, то есть
он строже. Подтверждение даёт именно A/B: там IR и legacy меряются
**в одном прогоне и в одном процессе**, поэтому межсессионный дрейф
машины сокращается в отношении и на вердикт не влияет. Все восемь
не-double строк A/B сохранили вердикт предыдущей итерации (семь
`IR FASTER`, `{:<10s}` — `EQUAL`): пять попали в полосу v1
(`{:10d}` 198.6–215.0% против v1 196.6–202.3%; `{:s}` 102.8–116.5%
против 108.6–115.8%; `%s` 46.5–55.4% против 43.4–62.4%; `%0*d`
170.9–191.2% против 175.7–183.8%; `{:<10s}` EQUAL 0.6–2.0% против
EQUAL 2.1–2.8%), две оказались лучше v1 (`{}` 99.3–110.2% против
91.2–97.7%; `%10d` 114.6–123.2% против 111.9–112.1%), и одна ниже
полосы v1 (`{:x}` 193.4–228.1% против 235.4–245.2%) — при этом всё ещё
почти втрое быстрее legacy, то есть от регрессии далеко. Вывод
«не-double пути не пострадали» опирается на этот же-прогонный
инструмент, а межсессионная матрица его лишь не опровергает.

Cold-секция: `format (cold)` RED 0.638 → 0.686 / 0.691 µs (+7.5% /
+8.3%); `format → sprintf (cold)` RED 0.478 → 0.510 / 0.508 µs (+6.7% /
+6.3%). Сырые числа хуже RED, но **меньше**, чем сдвиг контрольной
группы, — то есть cold движется вместе с машиной и после поправки на
шум «не хуже». Улучшения тут нет, и записывать его не следует.

ERROR ровно один в каждом прогоне — намеренный (`sprintf 7.0` на
минимальном int). Quick-режим: 40.0 с / 40.7 с (порог 60 с).

**Прецедент из предыдущей спеки (Task 11; её каталог спеки уже удалён,
RED-файла v1 больше не существует — числа ниже цитируются из
handoff'а той итерации и проверить их по файлу нельзя)** — тот же
эффект дрейфа между сессиями, только тогда он был замечен на
целочисленных строках: 19-значные значения (максимум/минимум `int64`) в
`{:d}`/`{:10d}`/`%10d` выглядели на 10–23% медленнее RED (например
`{:10d}` на макс. int: RED 0.392 µs → 0.435 µs). Регрессией это не
было — подтвердилось прямым откатом `char_sink.dart` к состоянию до
Task 11 на той же машине в ту же сессию (те же ~0.43 µs,
0.430–0.433 µs). Тогда пришлось откатывать код, чтобы это доказать;
на гейте double-op'ов ту же роль сыграла контрольная группа сторонних
раннеров (см. ловушки) — способ дешевле и не требует правок кода.
Заодно зафиксировано: `_BraceIntOp`/`_PrintfIntOp` не проходят через
single-string путь (для самого числа `writeString` не зовётся), а
единственная стоимость single-string режима на прочих путях — один
`null`-чек в начале `_materialize()` на каждый
`writeCharCode`/`writeCodeUnits`/`fill`/`writeMagnitude`, в пределах
шума.

## Проверки на HEAD

Тестовые и analyzer-числа ниже — из фактических прогонов на этом
`HEAD` в этой сессии. **Бенчмарк-строки — унаследованные**: они сняты
на гейте double-op'ов и в этой сессии не перезапускались (связка
hardening'а гейтилась A/B-прогонами Task 2, см. раздел выше).

```text
dart test (корень): 519 прошли        (401 после багфикс-раунда; +118
  от закалки: корпуса по test() на кейс + экземпляры реестров +
  printf-кэш)
dart test (benchmark/suite): 14 прошли  (бывший example; пины {:010,d}
  дописаны в существующие тесты, новых test() нет)
dart test -p node test/char_sink_test.dart test/template_ir_compile_test.dart
  test/template_ir_diff_test.dart test/template_ir_fuzz_test.dart
  test/js_number_dispatch_test.dart:
  57 прошли (12 + 16 + 21 + 4 + 4 js_number_dispatch — файл добавлен
  в node-набор этой сессией: баг web-цифр жил в непрогоняемой JS-зоне)
dart analyze lib test example: No issues found!
Фаззер отдельно (VM): 4 теста, ~0.9 с, 1600 кейсов, seed 20260805;
  гейты после corpus refresh: distinct 390 / 327 / 383 / 346 (пол 100),
  rendered 283 и 288 (пол 200) — те же на VM и на node
Полная матрица (benchmark.dart) quick: живой прогон ревью-раунда,
  41.6 с, ERROR ровно 1 (намеренный), 28 сценариев включая новый
  {:010,d} (проверка функциональная, не гейт)
--- ниже унаследовано с гейта double-op'ов, в этой сессии не пере-снято ---
A/B (template_ir_benchmark.dart): 2 живых прогона по 15 сценариев,
  0 RESULTS DIFFER, 0 LEGACY FASTER, все 5 double-строк IR FASTER
Полная матрица (benchmark.dart) quick на гейте double-op'ов: 2 живых
  прогона, 40.0 / 40.7 с, ERROR ровно 1 в каждом (намеренный)
vm.loadavg на замерах (записанные значения): A/B — 4.07 и 3.93;
  фикс-раунд (третий прогон матрицы) — 4.08 (порог гейта < 5)
```

## Ловушки и знания (важно)

- Двойная библиотека: смешение `package:format/...` и относительного
  `../lib/src/...` в одном файле создаёт ДВЕ копии библиотеки
  (разные канонические URI) с независимыми статиками и несовместимыми
  типами. Seam-тесты импортируют движок ТОЛЬКО через
  `package:format/src/engine.dart` (тот же URI, что у публичного
  экспорта). Не возвращать относительные импорты.
- Тесты кэша обязаны звать `debugClearTemplateCaches` в `setUp`.
- Замеры чувствительны к фону: перед замером проверять
  `sysctl -n vm.loadavg` (порог < 5). Абсолютные цифры между сессиями
  плавают (см. кейс с 19-значными int выше) — для решений о
  регрессии/улучшении надёжнее сравнение «до/после» на одной машине в
  одну сессию, а не только сверка с числами из старого RED-файла.
- **Метод контроля шума при сверке с RED-файлом** (отработан на гейте
  double-op'ов): в полной матрице есть раннеры `sprintf 7.0` и
  `format 2.0` — сторонние/старые пакеты, которых наши изменения не
  касаются вообще. Их сдвиг относительно RED и есть измеренный шум
  машины (на гейте — +9%). Сравнивать `format 3.0` с RED надо
  относительно этой контрольной группы, иначе общий фоновый сдвиг
  читается как регрессия. На гейте double-op'ов без этой поправки
  не-double строки (+8.6%) выглядели бы регрессией, хотя контроль
  сдвинулся ровно так же (+9.9%). **Но у метода есть порог
  обнаружения**: разброс самой контрольной группы (±9%) — это и есть
  наименьшая регрессия, которую межсессионная сверка способна увидеть.
  Утверждать по ней «регрессии нет» нельзя, корректно только
  «регрессии выше ~9% нет». Для более тонких вещей инструмент —
  A/B-бенчмарк (один прогон, один процесс, порог 5%), он от
  межсессионного дрейфа не зависит.
- **Проверять `vm.loadavg` ПОСЛЕ запуска, а не только до.** На фикс-раунде
  Task 5 прогон матрицы стартовал при приемлемой загрузке и попал под
  внезапный билд Android Studio (JVM на 1022% CPU, loadavg 22): сдвиг
  контрольной группы подскочил с +9% до +88%, double-строки
  «просели» с −76% до −48%. Числа выглядели правдоподобно и молча
  испортили бы baseline. Спасла именно контрольная группа — она сразу
  показала, что виновата машина. Прогон был выброшен и повторён при
  loadavg 4.08 (записанное значение фикс-раунда).
- **`sprintf` вариадичен**: `Format.sprintf(template, value1, ..., value10)`.
  Вызов `sprintf('%.0f', [2.5])` передаёт **список** как `value1` и
  падает с `UnsupportedFormatValueException`. Для списка значений —
  `vsprintf(template, values)`. Та же ловушка запрещена в бенчмарках
  (маршаллинг перекашивает замер) и легко ловится в разовых probe-скриптах.
- **Порядок ошибок в double-путях — контракт**: валидация precision в
  режиме `dartSdk` идёт до проверки `isFinite` (плохая точность на
  `nan` обязана бросить). Зафиксировано в обоих op'ах; при правках
  веток `write` порядок не переставлять.
- `CharSink` single-string инвариант: `_single != null ⇒ _length == 0`.
  Любой новый метод записи, который может быть добавлен в `CharSink` в
  будущем, обязан либо звать `_materialize()` первым, либо явно
  документировать, почему он безопасен без неё (иначе строка,
  ожидающая в `_single`, будет молча потеряна).
- **`dart format` расходится со стилем, которым закоммичен репозиторий
  (не чинить походя — это решение владельца).** Локальный форматтер
  SDK **3.12.2** переписал бы **21 файл** из 104 при `dart format .`
  (в узкой области `lib test example` — **15** из 77). Пере-снято в
  этой сессии после чистки бенчмарков, рабочее дерево чистое: удалённые
  7 файлов гейта в расходящийся набор не входили, поэтому знаменатели
  уменьшились (было 111 и 84), а числители не изменились. Важные детали:
  - `lib/` **не затронут вообще** — расходятся только тесты,
    бенчмарки и вендоренные baseline'ы (`benchmark/baselines/sprintf7`,
    `packages/format_intl`, `example/`), в том числе файлы, которых
    связка hardening'а не касалась.
  - Расхождение **предшествует** этой связке: в списке лежат
    `test/conversion_test.dart`, `test/double_format_test.dart`,
    `test/template_cache_test.dart` и другие нетронутые файлы. То есть
    репозиторий закоммичен более старым/другим форматтером, а не
    испорчен последними правками.
  - Характер правок косметический: новый форматтер **склеивает**
    вызовы, которые в репозитории разложены по строкам (например в
    `test/parity_harness.dart` — `expect(...)` из 5 строк в 1).
  - **CI в репозитории нет** (`.github/` отсутствует), так что это
    ничего не блокирует и никакой гейт не красит.
  - Поэтому нормализацию форматтера делать **отдельным коммитом и
    только по решению владельца**: сделанная попутно, она размажет
    диффы по 21 файлу и утопит содержательные правки. Пока не сделана —
    не запускать `dart format` на весь репозиторий перед коммитом.
- VM-only тесты (литерал max int не компилируется dart2js):
  `integer_format_test.dart`, `sprintf_integer_test.dart`.
- «%d дожат»: мемоизация внутреннего `_FormatSpec` целочисленного
  форматтера дала ноль в A/B (аллокация мелких объектов бесплатна) —
  изменение откачено. Правило проекта: нет performance GREEN — нет
  коммита. Это же правило применено в Task 11 (single-string sink) и на
  гейте double-op'ов: если бы хоть одна double-строка A/B не дала
  `IR FASTER` или появился бы `RESULTS DIFFER`, handoff и коммит
  результатов не делались бы — брифом предусмотрена ветка BLOCKED.

## Возможные следующие шаги (незакрытые резервы)

- ~~**Double-op'ы**~~ — **закрыто** этой спекой (Tasks 1–5), см. раздел
  «Double-op'ы» выше.
- **dtoa прямо в sink**: сейчас `_BraceDoubleOp`/`_PrintfDoubleOp`
  сначала получают готовое тело в `_AsciiFloat` (строка) и только потом
  отдают его в `_writeAsciiFloatDirect` → `sink.writeString`. Генерация
  цифр напрямую в `CharSink` (по образцу `writeMagnitude` у
  целочисленных op'ов) убрала бы промежуточную строку целиком. Это
  самый крупный оставшийся резерв в double-путях. Ориентир, где искать
  выигрыш: `{:%}` — самый слабый из double-сценариев на гейте
  (2.78–2.82x против 6.1–6.7x у `{:g}`), причина не исследовалась.
- **Пул sink'ов**: `CharSink` сейчас создаётся заново на каждый вызов
  `format()`/`vsprintf()` с ёмкостью `program.estimatedCapacity`.
  Пулинг/переиспользование буфера между вызовами не исследовался —
  потенциальный резерв для очень горячих циклов форматирования
  (нужен профиль, прежде чем браться).
- **Арифметический поиск паддинга для `{:0N,d}`** (отложено сознательно,
  2026-08-05). Фикс `fitRegroupedZeroPadding` для целых (корректная
  Python-ширина у «0-заполнение + группировка») сделал эту комбинацию
  дороже: `{:010,d}` 0.65 → ~1.6 µs/op (микрозамер до/после в одной
  сессии, VM) — бинарный поиск в `applyNumericWidth` на каждой пробе
  строит строку через `formatDigits`. Незатронутые пути в шуме
  (`{:,d}` ~0.33, `{:010d}` ~0.11 µs/op; A/B — все сценарии в шуме).
  Решено оставить: холодный fallback, редкая комбинация, тот же
  механизм, которым float-путь (`{:,.2f}`) пользовался всегда, а старые
  0.65 µs были ценой неверного вывода. Если профиль покажет комбинацию
  горячей, план (~25–35 строк): опциональный колбэк «длина
  сгруппированных D цифр» в `applyNumericWidth` — для ASCII `,`/`_`
  она чисто арифметическая (`D + (D - 1) ~/ k`, разделитель
  односимвольный, scalars == graphemes), пробы поиска становятся
  безаллокационными O(1), `formatDigits` зовётся один раз в конце;
  ожидаемый возврат к ~0.7–0.8 µs/op. Локальный `n`-путь (паттерны
  вида [3,2], многоюнитные цифры локали) остаётся на честном поиске —
  арифметика там неверна. Полностью замкнутую формулу без поиска не
  делать: третья копия логики группировки, налог на синхронизацию.
  Замерная база уже стоит: `{:010,d}` запинен в A/B
  (fallback-control, EQUAL 0.0%), в полной матрице (28-й сценарий,
  format 2.0 согласен: `00,001,234`) и юнит-пинами в
  `test/integer_format_test.dart`.
- **Удаление legacy-процессоров** — **не рекомендуется**. Резерв
  оставлен в списке только как явно закрытая опция: `formatWithoutIr()`
  в `_BraceProcessor`/`_PrintfProcessor` стал **несущим** — это
  одновременно оракул parity-фаззера (1600 кейсов на прогон, единственный
  источник вердикта «IR ведёт себя как раньше») и baseline
  A/B-бенчмарка (16 сценариев, из них 5 double-строк). Удаление убьёт
  сразу оба инструмента: фаззеру не с чем сравнивать, A/B-гейт
  перестаёт существовать. Браться можно лишь вместе с решением, чем
  заменить и оракула, и baseline (например, зафиксированными эталонными
  выводами и числами вместо живого сравнения) — а это дороже, чем
  экономия от удаления. Отдельный PR на будущее, если вообще.
- **Отложенные мелочи из ledger'а спеки** (`progress.md` в
  `.superpowers/sdd/2026-08-05-double-ops/` — файл локальный, под
  `.gitignore`, в клоне его не будет; 12 пунктов по Tasks 1–4) — ни одна
  не блокирует, все сознательно отложены. **Связка hardening'а закрыла
  из них первые четыре темы**; открытой осталась только косметика:
  - ~~*Хрупкость к расширению*: catch-all `_ =>` в switch по типу тела
    `_PrintfDoubleOp`~~ — **закрыто `0ced01f`** (явная `'g' || 'G'` +
    `StateError` в `default`).
  - ~~*Микро-аллокации*: `late final formatted`; аллокация контекста
    ради discard-пути валидатора~~ — **закрыто `696eb2a`**
    (`final formatted` в трёх местах; контекст аллоцируется только на
    реально отвергнутой precision).
  - ~~*Работа, которую можно перенести в компиляцию*: диспетч
    uppercase~~ — **закрыто `696eb2a`** (поле `uppercase` в обоих
    op'ах). Диспетч **генератора** по типу тела при этом остался на
    записи — запечён только uppercase.
  - ~~*Пробелы в покрытии*: `-0.04` для z-флага, переполнение BigInt в
    бесконечность, `{}` со `doubleSpecialValueSpelling: short`,
    grapheme-парити для double, `'%f'` с пустым списком значений~~ —
    **все пять закрыты `df6ac9b`** (тесты `double edge values keep
    parity`, `empty-spec specials keep parity for short spelling`,
    `printf doubles keep parity for graphemes and missing args`).
  - *Косметика* (**открыто**): имя compile-теста «dynamic nested specs stay on
    fallback» больше не описывает свой `double:f:p2`-пин; два
    `{}`-сценария A/B делят одну кэшированную программу (замеры не
    независимы, но запас на порядок больше эффекта); assert
    `contains('{:.2f}')` в A/B выполним одной compatible-строкой.
- ~~**Точечные находки финального ревью ветки**~~ — **все пять
  закрыты** связкой hardening'а (см. одноимённый раздел выше):
  - ~~Классификатор double: заменить рукописную копию правила
    `precision > 100000` прямой пробой `_validateDoubleSpec`~~ —
    **закрыто `0ced01f`** (`_rejectsHotDouble` пробует валидатор в
    `try/catch`).
  - ~~Printf body-switch: явная ветка `'g' || 'G'` и `StateError`
    в `default`~~ — **закрыто `0ced01f`** (это же снимает пункт
    «хрупкость к расширению» из ledger'а прошлой спеки).
  - ~~`%.2f` в режиме `dartSdk` со статической precision: запечь
    вердикт валидатора на компиляции~~ — **закрыто `696eb2a`**
    (`staticPrecisionRejected`; динамическая `%.*f` сохраняет живую
    пробу — вердикт на компиляции ей недоступен по определению).
  - ~~Дифф-матрица: кейс brace-double × кастомная (не-`C`) локаль~~ —
    **закрыто `df6ac9b`** (плюс короткие спеллинги, краевые значения и
    отказ по precision — статический и динамический).
  - ~~Рандомизированный parity-фаззер против legacy-оракула~~ —
    **закрыто `4128a3d`**: `test/template_ir_fuzz_test.dart`, seed
    `20260805`, **1600 кейсов** (400 × 4 теста), **6 движков**,
    гейты на вырождение корпуса — distinctness (`> 25%` уникальных
    шаблонов) и **success floor** (`> 50%` реально отрендерившихся
    кейсов в «matched»-тестах). ~0.84 с на VM.
- **Отложенные мелочи связки hardening'а** (не блокируют, ни одна не
  влияет на корректность продакшн-путей):
  - ~~*Кандидат на однострочный follow-up*: `pow(10, ...)` с двумя
    int-аргументами переполняет int64 на показателе 19~~ — **закрыто
    `7dd02d5`** (`pow(10.0, ...)` в обоих генераторах, отдельным
    коммитом как corpus refresh, гейты пере-проверены).
  - ~~Парити-хелперы кладут в `reason` только шаблон — без значения и
    без движка~~ — **закрыто `31e2659`** (опциональный `label`; фаззер
    передаёт индекс кейса, индекс движка и значение).
  - Динамическая `%.*f` покрыта парити-тестами (`df6ac9b`), но в
    **A/B-матрицу** (`example/lib/src/template_ir_benchmark.dart`) не
    заведена: там есть `%.2f` и `%0*d`, а связки «double × динамическая
    precision» нет. То есть выигрыш живой пробы из `696eb2a` ничем не
    измеряется.
  - ~~*Payload'ы исключений в харнесе*: сравнение `toString()` вакуумно,
    поля подклассов не проверяются ничем~~ — **закрыто `31e2659`**
    (`describeErrorPayload()` — exhaustive `switch` без `default`, плюс
    `label` у хелперов; расхождений не нашлось).
  - ~~*`pow(10.0, ...)` в генераторе значений фаззера* отдельным
    коммитом как corpus refresh~~ — **закрыто `7dd02d5`** (гейты
    пере-проверены на VM и node, числа не изменились).
  - ~~*`'{:.21%}'` в brace rejection-матрицу* дифф-теста~~ — **закрыто
    `a95fac4`** (плюс `'{:.0%}'` — вторая граница того же образца — и
    исправленный комментарий блока про f/g-образцы).
  - *Ветка `FormatExtensionException` в `describeErrorPayload()`* (открыто,
    сегодня недостижимо): сравнивается только имя расширения — добавление
    `error.runtimeType` закрыло бы слепое пятно обёрнутой ошибки, но ни один
    движок харнеса расширений не регистрирует, так что ветка не исполняется.
  - *Границы precision у `%` и пустой конверсии* (открыто): запинены только
    по парити (IR против legacy), направление — кто именно отвергает — не
    утверждается нигде; кандидат на 4 строки в
    `test/dart_double_format_test.dart`, где сейчас есть только `f/e/g`.
- Мелочи из более раннего handoff (декларативный timeout JS-теста,
  3 analyzer-info в `benchmark_scenarios_test.dart`) закрыты
  коммитами `0299a79` и `c759228` ещё до работы над IR.
