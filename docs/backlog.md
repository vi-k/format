# TODO

Рабочий бэклог пакета. Пункт, взятый в работу, уходит в спеку в
`docs/` (формат имён — `YYYY-MM-DD[N]-<тема>.md`) и вычёркивается
отсюда; сделанное живёт в CHANGELOG, а не здесь.

- расширить parity-фаззер: конверсии `o`/`b`/`X` (brace) и `o`/`X`/`c`
  (printf), именованные и вложенные поля, `!r`/`!s`, Unicode-значения,
  несколько плейсхолдеров. Требует пересъёмки порогов distinct/rendered
  корпуса на VM и node — отдельный заход
- при наличии графем в шаблоне корректно ли их кэшировать?
- везде, где можно, заменить List.unmodifiable на List.of(growable: false)
- убрать везде, где это возможно, тип в List.of, Map.of, Set.of
- посмотреть issues на github
