# TODO

Рабочий бэклог пакета. Пункт, взятый в работу, уходит в спеку в
`docs/` (формат имён — `YYYY-MM-DD[N]-<тема>.md`) и вычёркивается
отсюда; сделанное живёт в CHANGELOG, а не здесь.

- ~~Не можем ли мы передавать в canFormat, canLookup, canRepresent передавать
  сразу нужный тип, а проверку типа делать под капотом?~~ — **исполнено
  2026-08-14:** движок проверяет generic `T`, а typed-предикат остаётся для
  дополнительного условия внутри типа.
