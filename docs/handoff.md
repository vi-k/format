# Handoff: состояние проекта

Статус: живой документ, обновляется каждую сессию. Обновлён 2026-08-16.

**Здесь только состояние.** Правила работы, полный список проверок перед
коммитом и накопленные ловушки — в `docs/conventions.md`; устойчивое
устройство кода — в `docs/architecture.md`; бэклог владельца — в
`docs/backlog.md`; спеки, планы, отчёты и ревью — в `docs/records/`.

Рабочий каталог: `/Users/user/development/my/format`

## Состояние

**Ветка `main`, дерево чистое.** Мержим прямо в `main`, без PR.

**Опубликовано.** `format 4.0.0` — на pub.dev с 2026-08-15, собран из
коммита `8bbddb8`; тег `v4.0.0` и GitHub Release проверены на удалённом.
`format 3.0.0` от 2026-08-14 ретрактирована в тот же день — **отменить
ретракт можно до 2026-08-22**, после этого он окончателен; номер 3.0.0
не переиспользуется в любом случае. `format_intl` — `1.0.1`, его
ограничение на `format` — `>=3.0.0 <5.0.0`: адаптер реализует только
`NumberLocale`, которой мажор не касался, и работает на обоих.

Мажорной версию сделало одно изменение — типизированные предикаты
расширений (`canFormat`, `canLookup`, `canRepresent` принимают `T`).
Почему мажор, а не минор, разобрано исполнением в
`docs/records/2026-08-15[4]-format-reference-and-4-0-0-report.md`.

**SDK.** Внутренние wasm-host'ы, форматтер и primary jobs — на текущем
Dart stable (3.13.0). Минимальный поддерживаемый SDK — 3.7.2, у него
своя job.

**CI.** Workflow задаёт пять jobs на push и PR: `checks (3.7.2)`,
`checks (stable)`, `benchmark-suite`, `generated-artifacts`, `coverage`.
Шестая, `performance-gate`, идёт только по диспатчу и по расписанию.
Экшены запинены по коммиту, версия — в комментарии рядом; обновляет их
Dependabot (`.github/dependabot.yml`, раз в неделю, все пять одним PR).
Комментарий поэтому редактируется только вместе с SHA: бот читает его.

**Эталон performance-gate:** schema 2, ревизия
`9143e7407e162a1fdc6b11d57515143390a04c53`, две точные модели CPU —
`AMD EPYC 7763 64-Core Processor` (primary, снят 2026-08-14) и
`Intel(R) Xeon(R) Platinum 8370C CPU @ 2.80GHz` (2026-08-15). Обе
записаны восемью отчётами на Dart 3.13.0 и Node.js v24.8.0.

**Покрытие** — 96.04 % (3156/3286) при поле 94 %.

**Реестры вердиктов закрыты целиком:** `2026-08-05[5]` (30 из 30),
`2026-08-13[1]` (4 High, 17 Medium, 22 Low) и `2026-08-13[2]` (M1, L1).
Открытых находок ревью нет.

**Бэклог владельца пуст.**

## Что открыто

Активной implementation-работы нет. Ниже — только резервы, которые
никто не начинал, и состояние эталона гейта.

### Резервы производительности, не начатые

- **dtoa прямо в sink** — double-op'ы сперва получают готовое тело
  строкой и только потом отдают его в sink; генерация цифр напрямую
  убрала бы промежуточную строку. Резерв остался, но подешевел и
  сузился: с 2026-08-12 цифры `g`/`e`/пустого типа приходят от
  платформы, то есть промежуточных строк на этом пути теперь две — та,
  что вернула конверсия, и собранное из неё тело, — а не разбор в
  `BigInt`. Убирать имеет смысл вторую; первую без своего dtoa не
  убрать вовсе. Раскладка вокруг тела из этого резерва вычтена: для
  шаблона из одного поля она закрыта 2026-08-13, а там, где полей
  несколько, буфер уже оплачен и писать в него — правильно.
- **`Uint16List(16)` на пустой ёмкости** — программа из одного литерала
  получает `estimatedCapacity == 0` именно чтобы ничего не готовить, и
  всё равно аллоцирует буфер: 6.0 нс из 8.9 нс создания sink'а по
  декомпозиции 2026-08-13. Всплыло при разборе L7 и отложено как
  самостоятельная работа: путь одного литерала уже уходит в режим одной
  строки и буфера не касается, так что выигрыш достанется другим формам
  и его надо мерить отдельно.
- ~~Пул `CharSink`'ов~~ — **отклонён замером 2026-08-13**, разбор в
  `docs/records/2026-08-13[3]-third-review-and-cache-policy-report.md`.
- ~~Константы адаптивной политики кэша~~ — **подтверждены замером
  2026-08-13**, значения не изменились; разбор там же.

### Эталон гейта

Пересъёмка **не требуется прямо сейчас.** После записи в `lib/` легли
две правки — ускорение `c` под JavaScript и типизированные предикаты
расширений. Первая односторонняя, вторая матрицы не касается:
расширений в сценариях нет. `benchmark/scenarios.dart` после `9143e74`
не менялся, поэтому незнакомых id гейт не встретит.

Правило и его обоснование — в `docs/conventions.md`, раздел
`## Ловушки и знания`, пункт про односторонний эталон.
