# Первая волна по внешнему ревью: PR #14–#32

> **Состояние на 2026-08-16:** сделано и смержено; текст перенесён из
> `docs/handoff.md` без изменений.
> **Что это:** отчёт о работах 2026-08-07…09 — закрытие пунктов ревью
> `2026-08-05[5]` четырнадцатью PR, главные замеры и два отклонения.
> **Связанные записи:** `2026-08-05[5]-project-review.md`,
> `2026-08-10[2]-review-low-section-report.md`.

## Что сделано 2026-08-07…09

Проведено внешнее ревью
(`docs/records/2026-08-05[5]-project-review.md`), и дальше сессия
закрывала его пункты. **Статусы проставлены прямо в отчёте**,
у каждого закрытого пункта — замер и способ проверки. Коротко, по PR:

| PR | Что |
|---|---|
| #14–#16 | публичный API до публикации, холодные замеры как «кэш выключен», объединение классов бенчмарка |
| #17 | контракты расширений в dartdoc, разделы README, живые якоря реестра расхождений |
| #18 | горячий путь: канонизация C-локали, сканы вместо матчеров, группировка и `n` в IR |
| #19 | одинаковый отказ по слишком большой опции на VM и в вебе; предикаты `-0.0 is int` |
| #20 | разбор под dart2js: длинный литерал уходит нативному поиску |
| #21 | возврат арифметики заполнения в горячую ветку int-op |
| #22 | перезапись эталона гейта с CI-железа |
| #23–#25 | вывод матрицы: масштаб относительно format 1.6 вместо «OK» |
| #26–#27 | сверка handoff с бэклогом; описания всех тестов (37 файлов) |
| #28 | CI: сверка генераторов с артефактами, покрытие с полом |
| #29 | кэш шаблонов ограничен объёмом текста, не только числом записей |
| #30 | отметки закрытого в самом отчёте ревью |
| #31–#32 | шапки тестов — доккомментарии на `library`, ссылки через `[]` |

Главные числа (VM, минимум из раундов): `{:010,d}` ×8.9, `compatible {}`
×5.2, `%a` ×3.4, `{:,.2f}` ×3.1, `{:,d}` ×2.8, `%f` с не-`const`
`CNumberLocale` ×3.7. Под dart2js разбор литерала в 400 символов ×9.0.

Отклонены по замеру: **M16** (кэш `decimalPower` выше 400 — некэшируемый
`pow` зовётся один раз на форматирование и стоит 0.7 % от него) и **M11**
(`/packages/` в `.pubignore` ломает архив — поймано собственным гейтом).

