# Завершение релиза 3.0.0

> **Состояние на 2026-08-16:** исполнен 2026-08-14. `v3.0.0` и GitHub Release
> созданы на опубликованном коммите, issue #8 закрыта; чекбоксы в теле отражают
> состояние плана во время выполнения и не являются списком открытой работы.
> **Что это:** план завершения релиза 3.0.0 — тег, GitHub Release, закрытие
> issue #8.
> **Связанные записи:** `2026-08-01[7]-format-3-verification-release-plan.md`,
> `2026-08-14[11]-stable-migration-and-3-0-0-report.md`.

> **Для агентных исполнителей:** REQUIRED SUB-SKILL: Use
> superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Цель:** Завершить уже состоявшуюся публикацию обязательными тегом, GitHub
Release и закрытием issue #8, а постоянное правило записать в инструкции.

**Архитектура:** Опубликованные архивы неизменяемы и собраны из
`c58b2f37ff4e79956f866c17d6ee1c50194a9dc8`, поэтому `v3.0.0` указывает именно
на этот коммит, а не на последующий документационный handoff. Постоянная
обязанность живёт в `AGENTS.md`; текущее состояние и выполненный проход — в
`docs/handoff.md`.

**Технологии:** Git, GitHub CLI, GitHub Releases, pub.dev API, Markdown.

## Общие ограничения

- Работать прямо в `main`, без PR.
- Не включать `docs/backlog.md`.
- Тег `v3.0.0` должен разрешаться в опубликованный коммит `c58b2f3`.
- GitHub Release должен использовать существующий тег `v3.0.0`.
- Issue #8 закрывается только после проверки обеих версий через pub.dev API.

---

### Задача 1: Постоянное правило и живое состояние

**Файлы:**
- Изменить: `AGENTS.md`
- Изменить: `docs/handoff.md`
- Создать: `docs/records/2026-08-14[10]-release-housekeeping-plan.md`

**Интерфейсы:**
- Потребляет: подтверждённые версии `format 3.0.0` и `format_intl 1.0.0`.
- Производит: однозначный обязательный порядок завершения будущих релизов.

- [x] **Шаг 1: Добавить в `AGENTS.md` шестую обязанность**

  После успешного `dart pub publish` обязательно создать git-тег версии и
  GitHub Release на опубликованном коммите, проверить оба удалённых объекта и
  только затем считать релиз завершённым.

- [x] **Шаг 2: Обновить `docs/handoff.md`**

  Перенести тег, Release и issue #8 из открытого housekeeping в сделанное;
  следующим открытым делом оставить миграцию Dart 3.13.0.

- [x] **Шаг 3: Проверить документационный diff**

  Выполнить `git diff --check` и убедиться через `git status --short`, что
  изменены только три перечисленных документа.

### Задача 2: Коммит инструкций

**Файлы:**
- Изменить: только файлы задачи 1.

**Интерфейсы:**
- Потребляет: зелёный полный локальный прогон и CI на неизменившемся дереве
  исходников.
- Производит: документационный коммит в `origin/main`.

- [x] **Шаг 1: Подтвердить актуальную верификацию**

  Использовать завершённый полный прогон VM/node/wasm, архива, coverage и
  трёх benchmark-матриц и зелёный CI текущего дерева исходников; изменения
  после него должны быть только Markdown.

- [ ] **Шаг 2: Закоммитить документы**

  Добавить точные пути, проверить staged diff и создать коммит
  `docs: сделать тег и GitHub Release обязательными` с Dart 3.12.2 в `PATH`,
  чтобы pre-commit hook использовал закреплённый форматтер.

- [ ] **Шаг 3: Запушить `main`**

  Выполнить `git push origin main` и проверить синхронизацию ветки.

### Задача 3: Неизменяемые объекты релиза

**Файлы:**
- Не изменяются.

**Интерфейсы:**
- Потребляет: опубликованный source commit `c58b2f3` и changelog 3.0.0.
- Производит: удалённый тег и GitHub Release `v3.0.0`.

- [x] **Шаг 1: Создать аннотированный тег**

  Создать `v3.0.0` на полном SHA
  `c58b2f37ff4e79956f866c17d6ee1c50194a9dc8` и запушить только этот тег.

- [x] **Шаг 2: Проверить удалённый тег**

  Получить `refs/tags/v3.0.0^{}` через `git ls-remote` и сравнить peeled SHA с
  опубликованным source commit.

- [x] **Шаг 3: Создать GitHub Release**

  Создать непредварительный, нечерновой Release `format 3.0.0` из уже
  запушенного тега. В notes дать ссылки на обе версии pub.dev, назвать
  companion package, breaking locale change и исправление issue #8.

### Задача 4: Закрытие issue и финальная проверка

**Файлы:**
- Не изменяются.

**Интерфейсы:**
- Потребляет: доступные pub.dev версии и опубликованный GitHub Release.
- Производит: закрытый issue #8 и доказуемо завершённый релиз.

- [x] **Шаг 1: Закрыть issue #8 с комментарием**

  Указать, что `format 3.0.0` опубликован с `characters: ^1.3.0`, приложить
  ссылки на pub.dev и GitHub Release, затем закрыть issue.

- [ ] **Шаг 2: Независимо проверить внешнее состояние**

  Через pub.dev API проверить latest-версии `3.0.0` и `1.0.0`; через GitHub
  получить target тега, `isDraft=false`, `isPrerelease=false`, URL Release и
  `state=CLOSED` issue #8.

- [ ] **Шаг 3: Проверить репозиторий и CI**

  Убедиться, что `main` синхронен с `origin/main`, рабочее дерево чисто, а CI
  документационного коммита завершился успешно.
