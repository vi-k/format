name: example
description: Example for format.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.2

dependencies:
  ansi_escape_codes: ^3.1.2
  benchmark_harness: ^2.3.1
  characters: ^1.4.0
  format:
    path: ..
  intl: ^0.20.2
  meta: ^1.16.0
  sprintf7_baseline:
    path: ../benchmark/baselines/sprintf7

dev_dependencies:
  lints: ^5.0.0
  test: ^1.26.3
